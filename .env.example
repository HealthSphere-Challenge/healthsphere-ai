# Documentation template only: no model or runtime exists yet.
# Load only a trusted, validated local pipeline artifact selected during HS-011.
MODEL_ARTIFACT_PATH=
# Service-auth configuration awaits HS-002; no user/application DB credentials belong here.
