# Required opaque credential for backend-to-AI requests. Use a real secret outside Git.
HEALTHSPHERE_INTERNAL_API_TOKEN=replace-with-a-service-specific-secret
# Optional deployment override; defaults to the committed v1 artifact directory.
# HEALTHSPHERE_ARTIFACT_DIR=/absolute/trusted/artifact/directory
