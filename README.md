# 🧠 HealthSphere — Predictive AI & Machine Learning

## 📌 Overview

This repository contains the **Predictive Machine Learning component** of the **HealthSphere** platform.

HealthSphere is an AI-powered health platform designed to help users better understand and monitor their health through personalized health profiles, health metrics, medical document analysis, predictive risk assessment, and an AI-powered conversational assistant.

This repository focuses specifically on the **structured-data Machine Learning pipeline**.

Its main responsibilities are:

- Dataset collection and generation
- Data exploration and analysis
- Data preprocessing
- Feature engineering
- Machine Learning experimentation
- Model training
- Model evaluation
- Model explainability
- Model versioning
- Prediction API integration

> The predictive model is intended to provide informational health-risk assessments and does not provide a medical diagnosis.

---

# 🎯 Objective

The objective of the Predictive ML component is to transform structured health information into an interpretable health-risk assessment.

Conceptually:

```text
Patient Health Data
        ↓
Data Preprocessing
        ↓
Feature Engineering
        ↓
Predictive ML Model
        ↓
Risk Assessment
        ↓
Explainability
```

Example input features may include:

- Age
- Sex
- BMI
- Blood pressure
- Health observations
- Medical history
- Lifestyle information
- Relevant laboratory measurements

The exact features and prediction target will be determined during dataset exploration and model development.

---

# 🧬 Dataset

## Synthea

For the initial phase of the project, HealthSphere uses synthetic healthcare data generated by **Synthea**.

Synthea is an open-source synthetic patient generator that provides realistic but fictional electronic health records.

Using synthetic data allows the project to explore healthcare data structures without relying on real patient records.

### Initial dataset

The initial dataset contains a sample of synthetic patient records exported in CSV format.

Raw Synthea data is stored under:

```text
data/raw/synthea/
```

The dataset includes files such as:

```text
patients.csv
conditions.csv
observations.csv
medications.csv
allergies.csv
encounters.csv
procedures.csv
immunizations.csv
careplans.csv
devices.csv
imaging_studies.csv
supplies.csv
claims.csv
claims_transactions.csv
organizations.csv
providers.csv
payers.csv
payer_transitions.csv
```

Not all files will necessarily be used by the final Machine Learning model.

The most relevant datasets will be selected during the data exploration and feature-engineering stages.

---

# 🔎 Dataset Usage

Synthea data can be used in HealthSphere for several purposes:

### 1. Data exploration

Understand the structure and relationships between synthetic patient records.

### 2. Patient simulation

Provide realistic synthetic health profiles for development and testing.

### 3. Feature construction

Extract relevant health variables from multiple Synthea CSV files.

For example:

```text
patients.csv
      +
observations.csv
      +
conditions.csv
      +
medications.csv
      +
encounters.csv
      ↓
Feature Engineering
      ↓
ML-ready Dataset
```

### 4. Machine Learning experimentation

The processed dataset can later be used to train and evaluate predictive models.

### 5. Testing

Synthetic patients can be used to test the complete HealthSphere prediction workflow without exposing real patient information.

---

# 📂 Repository Structure

```text
healthsphere-ai/
│
├── data/
│   │
│   ├── raw/
│   │   └── synthea/
│   │       ├── patients.csv
│   │       ├── conditions.csv
│   │       ├── observations.csv
│   │       ├── medications.csv
│   │       ├── allergies.csv
│   │       ├── encounters.csv
│   │       └── ...
│   │
│   ├── processed/
│   │   └── .gitkeep
│   │
│   └── README.md
│
├── notebooks/
│   ├── 01_data_exploration.ipynb
│   ├── 02_data_preprocessing.ipynb
│   ├── 03_model_training.ipynb
│   └── 04_model_evaluation.ipynb
│
├── src/
│   │
│   ├── preprocessing/
│   ├── training/
│   ├── evaluation/
│   └── explainability/
│
├── models/
│   └── .gitkeep
│
├── app/
│   │
│   ├── main.py
│   │
│   ├── api/
│   │   └── prediction.py
│   │
│   ├── schemas/
│   │   └── prediction.py
│   │
│   └── services/
│       └── prediction_service.py
│
├── tests/
│
├── requirements.txt
├── .gitignore
└── README.md
```

---

# 🔄 Machine Learning Workflow

The planned ML development workflow is:

```text
Synthea Raw Data
        ↓
Data Exploration
        ↓
Data Cleaning
        ↓
Feature Engineering
        ↓
ML-ready Dataset
        ↓
Train / Validation / Test Split
        ↓
Model Training
        ↓
Model Evaluation
        ↓
Model Selection
        ↓
Explainability
        ↓
Serialized ML Pipeline
        ↓
Prediction API
```

---

# 🧹 Data Preprocessing

Raw Synthea data is preserved under:

```text
data/raw/
```

Raw files should **not be manually modified**.

Processed and transformed datasets will be generated under:

```text
data/processed/
```

A future processed dataset may look like:

```text
data/processed/health_risk_dataset.csv
```

The preprocessing pipeline may include:

- Missing-value handling
- Data cleaning
- Patient-level aggregation
- Categorical encoding
- Numerical scaling
- Feature engineering
- Feature selection
- Class balancing when required

---

# 🎯 Prediction Target

The final prediction target has **not yet been fixed during the initial dataset exploration phase**.

A possible HealthSphere output could later use categories such as:

```text
Low
Moderate
Needs Attention
```

However, these categories must not be created arbitrarily.

The final target definition must depend on:

- The selected dataset
- Available labels or outcomes
- Validated medical references
- The intended use of the predictive system

This decision will therefore be finalized before model training.

---

# 🤖 Model Development

Several interpretable Machine Learning algorithms may be evaluated, including:

- Logistic Regression
- Random Forest
- Other lightweight supervised-learning models if justified

Candidate models will be compared using appropriate evaluation metrics.

The final model will be selected based on experimental results rather than selecting an algorithm in advance.

---

# 📊 Model Evaluation

Depending on the final prediction task, evaluation may include:

- Precision
- Recall
- F1-score
- ROC-AUC
- Confusion Matrix

For health-risk classification, particular attention will be given to the model's ability to correctly identify cases requiring attention.

---

# 🔍 Explainability

HealthSphere should not return only a prediction.

Where technically appropriate, the predictive component should also provide information about the factors that influenced the model output.

Conceptually:

```text
Prediction
    ↓
Moderate Risk

Main contributing factors
    ↓
Blood pressure
BMI
Physical activity
...
```

The exact explainability technique will depend on the final selected model.

---

# 💾 Model Serialization

After training and validation, the final preprocessing pipeline and model will be serialized together.

Example:

```text
models/
└── health_risk_pipeline.joblib
```

The objective is to preserve:

```text
Preprocessing
      +
Feature Transformations
      +
Trained ML Model
      ↓
health_risk_pipeline.joblib
```

This ensures that the same transformations used during training are also applied during production inference.

---

# 🌐 Prediction API

The trained model will later be exposed through a lightweight FastAPI service.

Example:

```text
POST /predict
```

Conceptual flow:

```text
HealthSphere Backend
        ↓
POST /predict
        ↓
Predictive AI Service
        ↓
health_risk_pipeline.joblib
        ↓
Prediction
        ↓
HealthSphere Backend
```

The frontend does **not communicate directly with the ML service**.

The main HealthSphere backend remains responsible for orchestrating application requests.

---

# 🏗️ HealthSphere Integration

This repository is one component of the complete HealthSphere architecture:

```text
                    HealthSphere
                         │
            ┌────────────┼────────────┐
            │            │            │
            ▼            ▼            ▼
        Frontend       Backend     AI Services
         React         FastAPI
                                      │
                          ┌───────────┴───────────┐
                          │                       │
                          ▼                       ▼
                   Predictive ML              AI Agent
                   (this repo)          (separate repository)
```

The responsibilities are intentionally separated.

### `healthsphere-ai`

Responsible for:

```text
Structured health data
        ↓
Machine Learning
        ↓
Risk prediction
```

### `healthsphere-agent`

Responsible for:

```text
User conversation
      +
RAG
      +
Medical knowledge retrieval
      +
LLM
      ↓
Personalized explanations
```

The conversational AI does not generate or invent predictive risk scores. Predictive assessments must originate from the dedicated ML component.

---

# 🔐 Data Privacy

The initial dataset contains **synthetic patient records**.

No real HealthSphere user data should be committed to this repository.

Production user information will be stored securely by the main HealthSphere backend and database infrastructure.

Files containing:

- Real patient information
- Credentials
- API keys
- Database passwords
- Environment secrets

must never be committed to Git.

---

# 📌 Current Status — Phase 0

During Phase 0, this repository focuses on:

- [x] Dataset identification
- [x] Dataset collection
- [x] Initial repository architecture
- [x] Raw synthetic patient data
- [ ] Exploratory Data Analysis
- [ ] Data preprocessing
- [ ] Feature engineering
- [ ] Definition of prediction target
- [ ] Model training
- [ ] Model evaluation
- [ ] Explainability
- [ ] Model serialization
- [ ] Prediction API

---

# 🚀 Planned Next Steps

```text
1. Explore Synthea data
2. Identify relevant patient features
3. Build patient-level ML dataset
4. Define and justify prediction target
5. Split train / validation / test data
6. Train baseline models
7. Evaluate and compare models
8. Add model explainability
9. Export final ML pipeline
10. Integrate the model with FastAPI
```

---

# ⚠️ Medical Disclaimer

HealthSphere is designed as an informational and educational health-support platform.

The Machine Learning outputs generated by this project are **not medical diagnoses** and must not replace consultation with qualified healthcare professionals.